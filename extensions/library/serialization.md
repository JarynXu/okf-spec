# Serialization Status

The semantic model is normative before its concrete serialization is frozen. The first SDK may use an experimental manifest representation, but portable field names and the canonical manifest filename should not be declared stable until Local, Git, and Virtual profiles have been implemented and reviewed.

This prevents accidental coupling between the extension model and the first storage adapter.
