# Reference Boundary

The first SDK validates in-process abstractions. Remote wire protocols and OS-level virtual filesystem mounting remain adapters to be added after the capability model is proven; the SDK still supports virtual/generated nodes immediately through in-process providers.
