# Remaining Questions Before Stable v0.1

The semantic model is implemented before these are frozen:

- canonical manifest filename and exact serialization;
- remote provider wire protocol and version negotiation;
- dependency resolution semantics between Libraries;
- cache-control details for distributed providers;
- authorization vocabulary beyond capability gating;
- package registry/discovery protocol.

These are intentionally deferred until the reference SDK validates the core abstractions.
