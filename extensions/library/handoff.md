# Implementation Handoff

The semantic draft is frozen enough for SDK validation. Any SDK requirement that cannot be expressed by the current Library, Source, Provider, Catalog, Namespace, Query, Registry, or Lifecycle concepts should trigger a spec review rather than an ad-hoc storage-specific workaround.
