# Implementation Gate

SDK implementation may proceed. The architecture must be rejected or revised if the Local, Git, and fully Virtual reference profiles cannot share the same Runtime and Provider abstractions without storage-type branching in core routing logic.
